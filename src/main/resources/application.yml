spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/jpashop
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true  # sout?? ????.
        format_sql: true

#logger.level:
#  org.hibernate.SQL: debug # logger?? ???? ?? ??
logging:
  level:
    org.hibernate.SQL: debug # logger?? ???? ?? ??